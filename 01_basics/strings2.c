#include <stdio.h>
#include <string.h>

// using strlen to get the length of a string
int main() {

  char string1[20] = "DrongoCollege";
  printf("Length of string string1: %ld \n", strlen(string1));
  return 0;
}
