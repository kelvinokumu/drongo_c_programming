#include <stdio.h>

void main()
{
    int x = 125;
    if (x > 80 && x < 100)
    {
        printf("Grade is A \n");
    }
    else if (x > 70 && x < 80)
    {
        printf("Grade is B \n");
    }
    else
    {
        printf("Grade is C \n");
    }
}
