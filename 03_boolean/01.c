#include <stdio.h>
#include <stdbool.h>

void main()
{
    // bool access = true;
    // bool flag = true;
    bool x;

    int a = 115;
    int b = 25;
    x = (a < b);
    if (x)
    {
        printf("this is True \n");
    }
    else
    {
        printf("this is False \n");
    }
}